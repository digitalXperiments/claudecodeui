import { useEffect, useState } from 'react';
import { Bot as BotIcon, Eye, FileJson, X } from 'lucide-react';
import type { McItem } from '../../mission-control/api/missionControlApi';
import type { Bot } from '../types';
import StatusPill from '../ui/StatusPill';
import { Button } from '../../../shared/view/ui';

export default function ContextPane({ item, bot, preview, operatorContext, onOperatorContextChange, onBodyChange, onClose }: { item: McItem | null; bot?: Bot; preview: Record<string, unknown> | null; operatorContext: string; onOperatorContextChange: (value: string) => void; onBodyChange: (body: Record<string, unknown>) => void; onClose?: () => void }) {
  const [bodyDraft, setBodyDraft] = useState('');
  const [bodyError, setBodyError] = useState<string | null>(null);
  useEffect(() => { setBodyDraft(item ? JSON.stringify(item.body, null, 2) : ''); setBodyError(null); }, [item]);
  if (!item) return <aside className="flex min-h-0 flex-col border-t border-border/70 bg-card/30 xl:border-t-0"><div className="flex flex-1 flex-col items-center justify-center p-8 text-center"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-primary/10 text-primary"><BotIcon className="h-4 w-4" /></div><p className="mt-3 text-sm font-medium">Context pane</p><p className="mt-1 max-w-xs text-xs leading-5 text-muted-foreground">Select an inbox item or a bot to inspect its evidence, draft, and next tick.</p></div></aside>;
  const saveBody = () => { try { const parsed = JSON.parse(bodyDraft) as Record<string, unknown>; if (!parsed || Array.isArray(parsed)) throw new Error(); onBodyChange(parsed); setBodyError(null); } catch { setBodyError('Body must be a JSON object.'); } };
  return <aside className="flex min-h-0 flex-col border-t border-border/70 bg-card/30 xl:border-t-0"><div className="flex shrink-0 items-start gap-2 border-b border-border/70 px-4 py-3"><div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-xl bg-primary/10 text-primary">{bot?.icon || '🤖'}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{item.title}</p><div className="mt-1 flex items-center gap-2"><StatusPill status={item.status} /><span className="truncate text-[10px] text-muted-foreground">{bot?.title || item.section_id}</span></div></div>{onClose ? <Button size="sm" variant="ghost" onClick={onClose} aria-label="Close context pane"><X className="h-3.5 w-3.5" /></Button> : null}</div><div className="min-h-0 flex-1 space-y-4 overflow-y-auto p-4"><div><p className="text-[10px] font-semibold uppercase tracking-[0.16em] text-muted-foreground">Why this surfaced</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{item.summary || 'The bot produced this item during a tick.'}</p></div><div><label htmlFor="bot-steering-note" className="text-[10px] font-semibold uppercase tracking-[0.16em] text-muted-foreground">Steering note</label><textarea id="bot-steering-note" value={operatorContext} onChange={(event) => onOperatorContextChange(event.target.value)} placeholder="Add facts, corrections, tone, or the outcome you want…" className="mt-2 min-h-24 w-full resize-y rounded-xl border border-border bg-background px-3 py-2 text-xs leading-5 outline-none focus:border-primary focus:ring-2 focus:ring-primary/10" /><p className="mt-1 text-[10px] text-muted-foreground">Saved into the item body when you run an action.</p></div>{preview ? <div className="rounded-xl border border-primary/25 bg-primary/5 p-3"><div className="flex items-center gap-1.5 text-[10px] font-semibold uppercase tracking-[0.14em] text-primary"><Eye className="h-3 w-3" />Preview</div><pre className="mt-2 max-h-64 overflow-auto whitespace-pre-wrap break-words text-[10px] leading-4 text-muted-foreground">{JSON.stringify(preview, null, 2)}</pre></div> : null}<div><div className="flex items-center justify-between"><p className="text-[10px] font-semibold uppercase tracking-[0.16em] text-muted-foreground"><FileJson className="mr-1 inline h-3 w-3" />Edit before approve</p><Button size="sm" variant="ghost" onClick={saveBody}>Apply</Button></div><textarea value={bodyDraft} onChange={(event) => { setBodyDraft(event.target.value); setBodyError(null); }} className="mt-2 min-h-48 w-full resize-y rounded-xl border border-border bg-background px-3 py-2 font-mono text-[10px] leading-4 outline-none focus:border-primary focus:ring-2 focus:ring-primary/10" aria-label="Edit item body JSON" />{bodyError ? <p className="mt-1 text-[10px] text-destructive">{bodyError}</p> : null}</div></div></aside>;
}
