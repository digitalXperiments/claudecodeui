# Bot Studio — frontend core build spec

Detailed brief for the `impl:frontend-core` relay worker. Read fully before coding.

FACTS (from a read-only audit): `/studio` is route-driven: src/App.tsx:117-120 (`<Route path="/studio" element={<AppContent/>}>`), src/hooks/useProjectsState.ts:361-362 (`studioActive = pathname === '/studio' || startsWith('/studio/')`), 1013-1050 (enterStudio/leaveStudio + STUDIO_RETURN_SESSION_KEY in sessionStorage), 1241-1322 (exposed via sidebarSharedProps/return); AppContent.tsx:74-104,318-350 passes studioActive/onLeaveStudio to MainContent; MainContent.tsx:178-250 early-returns StudioView while keeping the normal ChatInterface mounted in a `className="hidden"` wrapper (copy this pattern exactly for botsActive); main-content/types/types.ts:41-69; sidebar types 62-88; Sidebar.tsx:70-123 holds showMissionControl state and the MC pending badge polling (missionControlApi.summary().pendingCount at 83-100), palette registration 242-249, MissionControlPanel render + WorkThisSessionRequest handoff 468-498 (opens a session via onNewSession + pending prompt); SidebarContent.tsx:675-704,707-748 threads rail props (same rail on mobile); SidebarRail.tsx:202-224 has the Action Centre RailButton (RailButton supports `active` and a numeric badge, 38-130); CommandPalette.tsx:301-329 has Action Centre/Studio commands; PaletteOpsContext.tsx:7-25,56-81 op types (openStudio/openMissionControl). StudioView.tsx layout to mirror: header 468-534, CSS grid var `--studio-grid` 447-455, resize handles 410-465 (clamps), asides 547-650 / 744-798, floating toggle 802-812. UI primitives: src/shared/view/ui/index.ts exports Alert, ActionMenu, Badge, Button, Confirmation, Card, Collapsible, Command, Dialog, Input, ScrollArea, Tooltip, PromptInput, PillBar, Queue (no Tabs/Select — build small local ones like other views do); icons: lucide-react; no global toast (use a local transient banner/toast state like src/components/file-tree/view/FileTree.tsx:34-49); mobile hook src/hooks/useDeviceSettings.ts. Mission Control client API: src/components/mission-control/api/missionControlApi.ts (summary, sections CRUD, run, items, actions, work, retry, preview, assets, draft-section, import). MCP inventory hook: src/components/mcp/hooks/useMcpCatalog.ts (GET /api/providers/mcp/inventory?phase=fast|full); MCP tools for a server: GET /api/providers/mcp/catalog/:name/tools → { tools: {name, description, inputSchema}[] }. Provider models are fetched in the old panel at MissionControlPanel.tsx:305-426 (copy that logic into a hook). i18n: only `actions.missionControl` exists in the 11 locale sidebar.json files (en 67-84, others ~65-69) — add `actions.botStudio` ("Bot Studio", localise reasonably) to ALL 11 and remove `actions.missionControl`.



SERVER CONTRACT (being added in parallel by another worker; code against it and degrade gracefully with optional chaining if a field is missing): McSection gains `tool_policy: Record<serverName, Record<toolName, 'allow'|'ask'|'deny'>>`; `GET /api/mission-control/summary` → `{ pendingCount, sectionCount, sections: [{ section_id, pending, failed, resolved_today, last_run_at, last_error }] }`; `GET /api/mission-control/sections/:id/runs?limit=` → `{ runs: [{ run_id, status, trigger, started_at, finished_at, duration_ms, error_summary, kind, item_id, tokens, cost_usd }] }`; `POST /api/mission-control/sections/bulk` `{ ids: string[]|'all', patch:{enabled} }` → `{ updated }`; system websocket events `mc_item_created`, `mc_item_updated`, `mc_section_updated` (find how the app subscribes to system events such as `interrupt_created` in src/ and subscribe the same way; fall back to a 30s poll).



CONTRACT with the parallel architect/templates worker — you import these and must NOT implement them; create MINIMAL compiling placeholders at exactly these paths so your worktree typechecks, and the lead will replace them at merge:

- src/components/bot-studio/architect/BotArchitect.tsx — `export interface BotArchitectProps { mode: 'create'|'edit'; initialSection?: Partial<CreateMcSectionInput> & { section_id?: string }; projects: Array<{ id: string; name: string; path: string }>; onSaved: (section: McSection) => void; onCancel: () => void } export default function BotArchitect(props: BotArchitectProps): JSX.Element`

- src/components/bot-studio/templates/BotTemplatesGallery.tsx — `export interface BotTemplate { key: string; name: string; icon: string; purpose: string; category: 'comms'|'engineering'|'content'|'ops'; requiredMcp: string[]; section: Partial<CreateMcSectionInput>; legacySeed?: boolean } export interface BotTemplatesGalleryProps { connectedMcpServers: string[]; onUse: (template: BotTemplate) => void } export default function BotTemplatesGallery(props: BotTemplatesGalleryProps): JSX.Element`

- `McSection` / `CreateMcSectionInput` come from src/components/mission-control/api/missionControlApi.ts — if those exact names do not exist there, add/export them additively, mirroring server/modules/mission-control/mission-control.types.ts:90-175 (include `tool_policy`).



DELIVERABLES:

1. Route + shell: `/bots` mirroring /studio end to end (botsActive, enterBots, leaveBots, return-session key, MainContent early return with hidden ChatInterface keep-alive, props threaded). Sidebar rail: ONE button "Bot Studio" (lucide `Bot` icon) replacing the Action Centre button; badge = pending items count (keep the summary polling, move it where sensible); active state when botsActive. Command palette: `openBotStudio` replaces openMissionControl. Remove the MissionControlPanel modal, its state and render from Sidebar.tsx; delete MissionControlPanel.tsx at the end (grep for remaining imports).

2. `src/components/bot-studio/` structure: `types.ts` (Bot view-model + `sectionToBot`/`botPatch` helpers implementing the vocabulary mapping incl. autonomy ⇄ mode/dry_run), `api/botStudioApi.ts` (thin wrapper over missionControlApi + the new endpoints), `hooks/useBotStudio.ts` (sections, items, summary, runs; refresh; optimistic item status; live events), `hooks/useBotStudioLayout.ts` (pane widths/collapse persisted in localStorage), `view/BotStudioView.tsx` (page), `view/BotStudioHeader.tsx`, `view/BotRoster.tsx` (left rail), `view/InboxView.tsx` + `view/InboxItemCard.tsx`, `view/BotDetailView.tsx` + `view/tabs/*.tsx`, `view/ContextPane.tsx` (right pane), `view/ActivityView.tsx`, `view/ImportView.tsx`, small local `ui/` (SegmentedControl, Tabs, Toggle, StatusPill, ToolPolicyGrid, Sparkline) if not available in shared ui.

3. Shell layout (from the prototype): header row = Back to chat, "Bot Studio" title + live pill, segmented Inbox·Bots·Templates·Activity with counts, search input (filters roster + inbox client-side), "Pause all / Resume all" (bulk endpoint) behind Confirmation, "+ New bot" (opens BotArchitect create), pane toggles. Grid = roster 280px | centre | context 400px with drag resize + collapse exactly like StudioView (clamps 220-360 / 320-560), stacked single column under xl using useDeviceSettings. Roster rows grouped Needs me (pending>0) · Healthy · Paused (enabled=false) · Failing (last_error) with icon, name, autonomy chip, next/last tick line, pending count. Sub-nav entries: Inbox, Activity, Templates, Import.

4. Inbox: unified across bots (filter chips Pending/Resolving/Resolved/Failed/All with counts; bot select). Card: bot chip, status pill, title, confidence bar, age, 2-line summary, source excerpt in a dashed amber "Source · untrusted content" box, draft preview if body.draft/reply exists, action buttons rendered from item.actions (primary for kind approve, ghost for dismiss, lock icon when the bot is in Propose mode and the kind is send-like), Preview (POST preview → result in the context pane), Retry for failed, "Work this ↗" (POST work → hand off via the existing WorkThisSessionRequest flow). Keyboard: J/K select, A first approve action, D dismiss, R first non-terminal reply action, Space multi-select; batch bar (approve all / dismiss all). Optimistic status → resolving → refresh. x_article bodies render with ArticleDraftCard. Steering note textarea in the context pane persisted into body.operatorContext (as the old panel did at MissionControlPanel.tsx:1301-1340) and an "Edit before approve" JSON editor.

5. Bot detail: header (icon, name, purpose = first line of produce_prompt, chips: scope/project/provider·model/cron/last tick; autonomy segmented control Dry run·Propose·Act that PUTs mode+dry_run; enabled toggle; Run now (POST run, shows created/skipped toast); Edit in Architect (opens BotArchitect edit); ⋯ menu: duplicate (POST sections with copied body, title + " copy"), delete (Confirmation)). Tabs: Overview (stats from summary + last runs + brief excerpt + agent card), Inbox (items for this bot), Brief (two editable textareas with Save → PUT; unsaved-changes guard), Tools (the bot's MCP servers = union of produce_tools/resolve_tools; per server: expandable tool list from /mcp/catalog/:name/tools with an allow/ask/deny 3-state per tool bound to tool_policy; "Add MCP server" dialog listing inventory servers not yet attached with connected/needs-auth state; phase toggles "used for produce / resolve / kanban"), Triggers (cron editor with presets + raw cron + human-readable summary, enabled toggle, permission mode select), Outputs & actions (action set table: label/kind/style/terminal reorder+edit+add; kanban bridge toggle with assignee/review provider selects + kanban MCP tools; swarm-on-approve toggle (project scope only)), Ticks (runs from the new endpoint; click → context pane shows the run summary with a link to the Runs view if a route exists), Danger (delete).

6. Activity: cross-bot ticks table (merge runs of all sections, newest first; lazy-load per section) and Import view (POST /import + default-path; migration report table).

7. Templates view renders <BotTemplatesGallery connectedMcpServers onUse> and on use opens <BotArchitect mode='create' initialSection={template.section}>.

8. Tests: add at least `src/components/bot-studio/types.test.ts` (mapping both directions, autonomy edge cases) and one hook/util test, runnable via the repo's client test command (package.json test:client uses tsx --test globs; you may NOT edit package.json, so name/place tests to match an existing glob and state which).

9. Accessibility + polish: focus rings, aria-labels on icon buttons, empty states, loading skeletons, error banners, dark and light via tokens only. Reduce clutter relative to the old panel: one primary action per region.